//GA 7th fizzbuzz
#include <stdio.h>

int main(){
    for (int x = 1; x <= 50; x++){
        if (x % 3 == 0 && x % 5 == 0) {
            printf("Fizbuzz\n");
        } else if (x % 3 == 0) {
            printf("fiz\n");
        } else if (x % 5 == 0) {
            printf("Buz\n");
        } else { 
            printf("%d\n", x);
        }
    }
    return 0;
}